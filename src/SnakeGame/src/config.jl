const KeyMap = (
    UP      = ["W", "UP"],
    DOWN    = ["S", "DOWN"],
    LEFT    = ["A", "LEFT"],
    RIGHT   = ["D", "RIGHT"],
)

const Settings = (
    framerate = 10,
    resolution = 20,
    colors = (
        bg = "#111",
        snake = "#eee",
        gameover = "red"
    )
)

const Dir = (
    UP    = [ 0, -1],
    DOWN  = [ 0,  1],
    LEFT  = [-1,  0],
    RIGHT = [ 1,  0]
)